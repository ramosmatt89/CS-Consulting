
export interface Service {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface Testimonial {
  id: string;
  name: string;
  type: string;
  comment: string;
  rating: number;
}

export interface Benefit {
  id: string;
  title: string;
  icon: string;
}
